map:{
    # test of split style, you can organize freeGen settings like this:
    ; $$split$$ = map:{
        ; basic = dummy
        ; custom = dummy
        ; lasta = dummy
        ; solr = dummy
        ; tricky = dummy
    }

    # also you can directly define freeGen requests here:
    #; BasicSingleJsonMapEmptyExampleGen = map:{
    #    ...
    #}
}